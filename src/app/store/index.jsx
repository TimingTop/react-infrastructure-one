import {createStore, applyMiddleware} from 'redux';

import allReducers from '../reducers';

let store = createStore(allReducers);

function createStoreWithMiddleware(reducers) {
    if (process.env.)
}

export default config => {
    return store;
}
